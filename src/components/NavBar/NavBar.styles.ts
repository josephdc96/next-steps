import { createStyles } from '@mantine/core';

export default createStyles((theme) => ({
  Navbar: {},
  user: {
    height: 60,
  },
  userInfo: {
    width: 200,
    textAlign: 'left',
  },
  colorSchemeToggle: {
    marginLeft: '1.25rem',
    marginTop: '.5rem',
  },
}));
